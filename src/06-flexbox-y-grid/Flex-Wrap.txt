Flex-Wrap
Utilidades para controlar cómo se envuelven los elementos flexibles.

Clase |  Propiedades
flex-wrap  |  flex-wrap: wrap;
flex-wrap-reverse  |  flex-wrap: wrap-reverse;
flex-nowrap  |  flex-wrap: nowrap;

flex-nowrap
Use flex-nowrap para evitar que los artículos flexibles se envuelvan, lo que hace que los artículos inflexibles desborden el contenedor si es necesario:

...

Flex-Wrap
Use flex-wrap para permitir que los artículos flexibles se envuelvan:

...

Flex-wrap-reverse
Utilice flex-wrap-reverse para envolver elementos flexibles en la dirección inversa:

...